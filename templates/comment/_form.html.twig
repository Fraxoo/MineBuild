{{ form_start(form, {
    attr: {
        class: 'w-full flex flex-col gap-2',
    }
}) }}

{{ form_label(form.content, 'Commentaire', {'label_attr': {'class': 'sr-only'}}) }}
{{ form_widget(form.content, {
    attr: {
        class: 'w-full border border-(--border-color)  bg-(--container-color-secondary) px-4 py-2 rounded text-(--title-color) focus:outline-none focus:ring-2 focus:ring-(--primary-color) focus:ring-opacity-50',
    }
}) }}

<button class="primary-button w-fit">Commenter</button>

{{ form_end(form) }}
