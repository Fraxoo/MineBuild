{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

{% set usernameHasError = registrationForm.username.vars.errors|length > 0 %}
{% set emailHasError = registrationForm.email.vars.errors|length > 0 %}
{% set passwordHasError = (registrationForm.plainPassword.vars.errors|length > 0)
or (registrationForm.plainPassword.first.vars.errors|length > 0)
or (registrationForm.plainPassword.second.vars.errors|length > 0)
%}

<div class="middle-container-div">
    <div class="middle-container items-center">
        <div class="flex  flex-col items-center gap-8">
            <twig:WebsiteTitle />
            <div class="flex flex-col items-center gap-4">
				<h1 class="text-xl text-(--title-color) font-bold">Créer un compte</h1>
                <p class="text-(--secondary-color) text-center">Rejoignez la communauté et commencez à partager vos
                    créations !</p>
            </div>
        </div>
        {{ form_start(registrationForm , {
        'attr': {'class': "form-div w-full"}
        }) }}

        <div class="input-div">
			{{ form_label(registrationForm.username, "Nom d'utilisateur", {'label_attr': {'class': 'input-title'}}) }}
            <div class="input-input {{ usernameHasError ? 'input-input--error' : '' }}">
                <i class="ti ti-user"></i>
                {{ form_widget(registrationForm.username , {
                'attr': {'class' : "input"}
                }) }}
            </div>
            <div class="error-message">
                {{ form_errors(registrationForm.username) }}
            </div>
        </div>
        <div class="input-div">
			{{ form_label(registrationForm.email, 'Email', {'label_attr': {'class': 'input-title'}}) }}
            <div class="input-input {{ emailHasError ? 'input-input--error' : '' }}">
                <i class="ti ti-mail"></i>
                {{ form_widget(registrationForm.email , {
                'attr': {'class' : "input"}
                }) }}
            </div>
            <div class="error-message">
                {{ form_errors(registrationForm.email) }}
            </div>
        </div>
        <div class="input-div">
			{{ form_label(registrationForm.plainPassword.first, 'Mot de passe', {'label_attr': {'class': 'input-title'}}) }}
            <div class="input-input {{ passwordHasError ? 'input-input--error' : '' }}">
                <i class="ti ti-lock"></i>
                {{ form_widget(registrationForm.plainPassword.first , {
                'attr': {'class' : "input"}
                }) }}
            </div>
            <div class="error-message">
                {{ form_errors(registrationForm.plainPassword.first , {
                'attr': {'class' : "error-message"}
                }) }}
            </div>
        </div>
        <div class="input-div">
			{{ form_label(registrationForm.plainPassword.second, 'Confirmer le mot de passe', {'label_attr': {'class': 'input-title'}}) }}
            <div class="input-input {{ passwordHasError ? 'input-input--error' : '' }}">
                <i class="ti ti-lock"></i>
                {{ form_widget(registrationForm.plainPassword.second , {
                'attr': {'class' : "input"}
                }) }}
            </div>
            <div class="error-message">
                {{ form_errors(registrationForm.plainPassword.second) }}
            </div>

        </div>
        <div class="error-message">
            {{ form_errors(registrationForm.plainPassword) }}
        </div>
        <div class="flex check items-center gap-2 text-(--title-color)">
            {{ form_widget(registrationForm.agreeTerms) }}
			<label for="{{ registrationForm.agreeTerms.vars.id }}">J'accepte les</label>
			<a href="{{ path('app_cgu')}}" class="text-(--primary-color) hover:underline">conditions d'utilisation</a>
        </div>
        <div class="error-message">
            {{ form_errors(registrationForm) }}
        </div>
        <button type="submit" class="btn  primary-button">Créer un compte</button>
        <div class="border-t border-(--border-color) w-full pt-4">
            <p class="text-(--secondary-color)  text-center">Déja un compte ? <a
                    href="{{ path('app_login') }}" class="text-(--primary-color) hover:underline">Se connecter</a></p>

        </div>
        {{ form_end(registrationForm) }}
    </div>
</div>
{% endblock %}
