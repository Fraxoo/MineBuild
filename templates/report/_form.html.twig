{{ form_start(form , { 'attr' : { 'class' : 'build-form' } }) }}



<div class="flex flex-col gap-2">
	{{ form_label(form.reason_code, 'Raison du signalement', {'label_attr': {'class': 'text-(--title-color) font-bold text-xl'}}) }}
    {{ form_widget(form.reason_code)}}
</div>
<div class="flex flex-col gap-2">
	{{ form_label(form.message, 'Détails', {'label_attr': {'class': 'text-(--title-color) font-bold text-xl'}}) }}
    {{ form_widget(form.message)}}
</div>


<button class="btn primary-button text-lg w-full">
    <i class="ti ti-flag"></i>
    <span>Signaler</span>
</button>
{{ form_end(form) }}
