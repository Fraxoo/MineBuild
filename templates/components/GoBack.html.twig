<a{{ attributes }} class="flex gap-2 text-(--secondary-color) items-center font-bold" href="{{ app.request.headers.get('referer') ?: path('app_home') }}">
    <i class="ti ti-chevron-left"></i>
    <p>Retour</p>
</a>