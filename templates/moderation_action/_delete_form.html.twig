<form method="post" action="{{ path('app_moderation_action_delete', {'id': moderation_action.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moderation_action.id) }}">
    <button class="btn">Delete</button>
</form>
